using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using AddressMatch;

namespace AddressMatch.Test
{
    public class DumpTest
    {
        public static void DumpToFile()
        {
            //AddrSet set = new AddrSet(true);
            //AddrSet.AddrGraph.root = new GraphNode("root",LEVEL.Root);
            //AddrSet.AddrGraph.root.NextNodeList.Add(new GraphNode("A", LEVEL.Province));
            //AddrSet.AddrGraph.root.NextNodeList.Add(new GraphNode("B", LEVEL.Province));
            //set.Dump();
        }
        

    }
}
